@page
@model Quran_Memorizing_System.Pages.LessonsSearchModel
@{
}
@section Styles {
        <link rel="stylesheet" href="~/css/lessons.css" asp-append-version="true" />
}

<div class="container pt-3">
    <div class="header d-flex justify-content-between align-items-center mb-4">
        <h2>Lessons</h2>
        @if (!User.IsInRole("Sheikh"))
        {
                <a asp-page="/AddLesson" class="btn btn-success float-end">
                    <i class="bi bi-plus-circle me-1"></i> Add Lesson
                </a>
        }
    </div>

    <div class="search-wrapper mb-4 d-flex">
        <input type="text" id="searchBox" class="form-control" placeholder="Search lessons..." />
        <button class="btn btn-dark" onclick="searchLessons()">Search</button>
    </div>

    <div class="lessons-list">
        @foreach (var lesson in Model.Lessons)
        {
                <div class="lesson-card">
                    <div class="lesson-details">
                        <h3>@lesson["Title"]</h3>
                        <p><strong>Location:</strong> @lesson["Location"]</p>
                        <p><strong>Availability:</strong> @lesson["Availability"]</p>
                    </div>
                    <div class="lesson-actions d-flex justify-content-end">
                        <a asp-page="/ViewLesson" asp-route-id="@lesson["Lesson_ID"]" class="btn btn-dark me-2">View</a>
                    @if (!User.IsInRole("Sheikh"))
                    {
                                <a asp-page="/EditLesson" asp-route-Lesson_ID="@lesson["Lesson_ID"]" class="btn btn-warning me-2">Edit</a>      
                                <form method="post" asp-page-handler="Delete" class="d-inline">
                                    <input type="hidden" name="lessonId" value="@lesson["Lesson_ID"]" />
                                    <button type="submit" class="btn btn-danger">Delete</button>
                                </form>
                    }
                    </div>
                </div>
        }
    </div>
</div>
@* @section Styles {
        <link rel="stylesheet" href="~/css/lessons.css" asp-append-version="true" />
}

<div class="container row pt-3">

    <!-- Add Lesson button (Sheikh only) -->
    @if (!(User.Identity.IsAuthenticated && User.IsInRole("Sheikh")))
    {
            <div class="mb-4">
                    <a asp-page="/Add_Lesson" class="btn btn-success px-4">
            <i class="bi bi-plus-circle me-1"></i> Add Lesson
        </a>
            </div>
    }

    <!-- Left: Search + lessons -->
    <div class="col-lg-8">
        <!-- Search -->
        <div class="search-wrapper mb-4 d-flex align-items-center">
            <input type="text" id="searchBox"
                   class="form-control border-0 shadow-0"
                   placeholder="Search lessons">
            <button class="btn btn-dark ms-3 px-4" onclick="searchLessons()">
                <i class="bi bi-search me-1"></i> Enter
            </button>
        </div>

        <!-- Lesson cards -->
        @foreach (var lesson in Model.Lessons)
        {
                <div class="lesson-card">
                    <div class="d-flex align-items-start">
                        <div class="lesson-radio"></div>
                        <div>
                            <div class="lesson-label">@lesson["instrutor_email"]</div>
                            <div class="lesson-title">@lesson["Title"]</div>
                            <div class="small fw-semibold">Location: @lesson["Location"] | Availability: @lesson["Availability"]</div>
                        </div>
                    </div>
                    <div class="mt-2">
                        
                        <a class="btn btn-dark watch-btn" href="/ViewLesson?lessonId=@lesson["Lesson_ID"]">View</a>

                    @if (!(User.Identity.IsAuthenticated && User.IsInRole("Sheikh")))
                    {
                                <a class="btn btn-warning watch-btn" href="/EditLesson?lessonId=@lesson["Lesson_ID"]">Edit</a>
                                <form method="post" asp-page-handler="Delete">
                                      <input type="hidden" name="lessonId" value="@lesson["Lesson_ID"]" />
                                      <button type="submit" class="btn btn-danger">Delete</button>
                                              </form>

                                
                    }
                    </div>
                </div>
        }
    </div>

</div>
 *@
<script>
function searchLessons() {
    let searchValue = document.getElementById("searchBox").value.toLowerCase();
    let cards = document.querySelectorAll(".lesson-card");
    cards.forEach(card => {
        let title = card.querySelector(".lesson-title").innerText.toLowerCase();
        card.style.display = title.includes(searchValue) ? "block" : "none";
    });
}
</script>


@* @section Styles {
        <link rel="stylesheet" href="~/css/lessons.css" asp-append-version="true" />
}
<div class="container row pt-3">
    <div class="col-lg-8">
        <div class="mb-4 d-flex justify-content-between align-items-center">
            <h3>Lessons</h3>
            <a href="/Add_Lesson" class="btn btn-success px-4">
                <i class="bi bi-plus-circle me-1"></i> Add Lesson
            </a>
        </div>
        <!-- Search bar -->
<form method="get" class="mb-4 d-flex align-items-center gap-2">
    <input type="text" name="SearchTerm" value="@Model.SearchTerm" 
           class="form-control border-0 shadow-0" placeholder="Search lessons by title">
    <button type="submit" class="btn btn-dark px-4">
        <i class="bi bi-search me-1"></i> Search
    </button>
</form>
        @foreach (var lesson in Model.Lessons)
        {
                <div class="lesson-card mb-3 p-3 border rounded shadow-sm d-flex justify-content-between align-items-center">
                    <div>
                        <div class="fw-bold">@lesson.Title</div>
                        <div class="small text-muted">Location: @lesson.Location</div>
                        <div class="small text-muted">Availability: @lesson.Availability</div>
                    </div>

                    <div class="d-flex gap-2">
                        <a class="btn btn-primary px-3" href="/EditLesson?Lesson_ID=@lesson.Lesson_ID">
                            <i class="bi bi-pencil-square me-1"></i> Edit
                        </a>
                        <a class="btn btn-info px-3" href="/ViewLesson?Lesson_ID=@lesson.Lesson_ID">
                            <i class="bi bi-eye me-1"></i> View
                        </a>
                        <form method="post" class="d-inline">
                            <input type="hidden" name="lessonId" value="@lesson.Lesson_ID" />
                            <button type="submit" class="btn btn-danger px-3" formaction="?handler=Delete">
                                <i class="bi bi-trash me-1"></i> Delete
                            </button>
                        </form>
                    </div>
                </div>
        }
    </div>
</div> *@

@* <div class="container row pt-3">
    <!-- Left: Search + lessons -->
    <div class="col-lg-8">
        <!-- Top: Add Lesson Button -->
        <div class="mb-4 d-flex justify-content-between align-items-center">
            <h3>Lessons</h3>
            <a href="/Add_Lesson" class="btn btn-success px-4">
                <i class="bi bi-plus-circle me-1"></i> Add Lesson
            </a>
        </div>

        <!-- Search -->
        <div class="search-wrapper mb-4 d-flex align-items-center">
            <input type="text" class="form-control border-0 shadow-0" placeholder="Search">
            <button class="btn btn-dark ms-3 px-4">
                <i class="bi bi-search me-1"></i> Enter
            </button>
        </div>

        <!-- Lesson cards -->
        @foreach (var lesson in Model.Lessons)
        {
                <div class="lesson-card">
                    <div class="d-flex align-items-start justify-content-between">
                        <div>
                            <div class="lesson-title fw-bold">@lesson.Title</div>
                            <div class="small text-muted">Location: @lesson.Location</div>
                            <div class="small text-muted">Availability: @lesson.Availability</div>
                        </div>
                        <div class="d-flex gap-2">
                            <a class="btn btn-primary" href="/EditLesson?Lesson_ID=@lesson.Lesson_ID">Edit</a>
                            <a class="btn btn-info" href="/ViewLesson?Lesson_ID=@lesson.Lesson_ID">View</a>
                        </div>
                    </div>
                </div>
        }
    </div>

    <!-- Right: Filters (optional) -->
    <div class="col-lg-4 mt-4 mt-lg-0">
        <h5 class="filters-title">FILTERS</h5>
        <form class="filters-box">
            <button class="btn btn-dark watch-btn mt-2">Apply</button>
        </form>
    </div>
</div> *@


@* @section Styles {
        <link rel="stylesheet" href="~/css/lessons.css" asp-append-version="true" />
}

<div class="container row pt-3">

    <!-- Header -->
    <div class="col-12 mb-3 d-flex justify-content-between align-items-center">
        <h4 class="fw-bold">Lessons</h4>
        <!-- Add Lesson Button -->
        <a asp-page="/Add_Lesson" class="btn btn-success px-4">
            <i class="bi bi-plus-circle me-1"></i> Add Lesson
        </a>
    </div>

    <!-- Left: Search + Lessons -->
    <div class="col-lg-8">

        <!-- Search -->
        <form method="get" class="search-wrapper mb-4 d-flex align-items-center">
            <input type="text"
                   name="Search"
                   value="@Model.Search"
                   class="form-control border-0 shadow-0"
                   placeholder="Search lessons">
            <button type="submit" class="btn btn-dark ms-3 px-4">
                <i class="bi bi-search me-1"></i> Search
            </button>
        </form>

        <!-- Lesson cards -->
        @foreach (var lesson in Model.Lessons)
        {
                <div class="lesson-card">
                    <div class="d-flex align-items-start">
                        <div class="lesson-radio"></div>
                        <div>
                            <div class="lesson-label">@lesson.Instructor</div>
                            <div class="lesson-title">@lesson.Title</div>
                            <div class="small fw-semibold">
                                (@string.Join(", ", lesson.Tags))
                            </div>
                        </div>
                    </div>

                    <!-- Action buttons -->
                    <div class="mt-3 d-flex gap-2">
                        <!-- View -->
                        <a asp-page="/ViewLesson"
                           asp-route-id="@lesson.Id"
                           class="btn btn-dark btn-sm">
                            <i class="bi bi-eye"></i> View
                        </a>

                        <!-- Edit -->
                        <a asp-page="/EditLesson"
                           asp-route-id="@lesson.Id"
                           class="btn btn-warning btn-sm text-white">
                            <i class="bi bi-pencil"></i> Edit
                        </a>

                        <!-- Delete -->
                        <form method="post"
                              asp-page-handler="Delete"
                              asp-route-id="@lesson.Id"
                              onsubmit="return confirm('Are you sure you want to delete this lesson?');">
                            <button type="submit" class="btn btn-danger btn-sm">
                                <i class="bi bi-trash"></i> Delete
                            </button>
                        </form>
                    </div>
                </div>
        }
    </div>

    <!-- Right: Filters -->
    <div class="col-lg-4 mt-4 mt-lg-0">
        <h5 class="filters-title">FILTERS</h5>

        <form class="filters-box">
            <div class="form-check mb-2">
                <input class="form-check-input" type="checkbox" id="tag1">
                <label class="form-check-label" for="tag1">Tag One</label>
            </div>

            <div class="form-check mb-2">
                <input class="form-check-input" type="checkbox" id="tag2">
                <label class="form-check-label" for="tag2">Tag Two</label>
            </div>

            <button type="button" class="btn btn-dark watch-btn mt-2">
                Apply
            </button>
        </form>
    </div>

</div> *@